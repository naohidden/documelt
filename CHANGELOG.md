# Changelog

このプロジェクトの主な変更点を記録します。

## [0.2.0]

### Added

- **Markdown 出力** — `extract()` / `extractFromFile()` に `options` 引数を追加。`{ format: 'markdown' }` を指定すると `texts` の各要素が Markdown 文字列になります。

  ```typescript
  const result = await extractFromFile(file, { format: "markdown" });
  console.log(result.texts); // ページ単位の Markdown 配列
  console.log(result.texts.join("\n")); // 全文
  ```

  ページ・スライド・シートは配列の要素として表現され、`## Page 1` のような見出しは本文に挿入されません。

- 変換される要素:

  | 要素        | DOCX |     PPTX     |     XLSX     | PDF / TXT |
  | ----------- | :--: | :----------: | :----------: | :-------: |
  | 見出し      |  ○   | ○ (タイトル) | ○ (シート名) |    －     |
  | 表          |  ○   |      ○       |      ○       |    －     |
  | リンク      |  ○   |      ○       |      ○       |    －     |
  | 太字 / 斜体 |  ○   |      ○       |      ○       |    －     |
  | 取り消し線  |  ○   |      ○       |      －      |    －     |
  | ハイライト  |  ○   |      ○       |      －      |    －     |
  | リスト      |  ○   |      ○       |      －      |    －     |

- `ExtractOptions` 型をエクスポート。

### Changed

**API に破壊的変更はありません。** `ExtractionResult` の形・既存関数のシグネチャは 0.1.1 と同一で、追加した引数はすべて省略可能です。既存コードは無改修で動作します。

ただし `format` を省略した場合の**プレーンテキスト出力の内容**が一部変わります。抽出処理を構造化ブロック経由に作り直したためです。同一サンプルで 0.1.1 と比較した結果:

| 形式 | 変化   | 内容                                                                                |
| ---- | ------ | ----------------------------------------------------------------------------------- |
| TXT  | なし   | 完全一致                                                                            |
| PDF  | なし   | 完全一致                                                                            |
| DOCX | 軽微   | 空段落が出力されなくなった                                                          |
| XLSX | あり   | 先頭のシート名から `## ` が外れた（プレーンテキストに Markdown 記法を混ぜないため） |
| PPTX | 大きい | **段落ごとに改行が入るようになった**                                                |

- **PPTX の改行** — 0.1.1 では1スライドの全テキストが改行なしで1行に連結されていました（不具合）。0.2.0 では段落単位で改行されます。旧出力を行単位でパースしている場合は影響します。
- **XLSX のシート名** — `## SheetName` → `SheetName`。`## ` をシート境界の目印にしている場合は影響します。Markdown が必要な場合は `format: 'markdown'` を使ってください。

### Fixed

- PPTX の抽出結果で段落の改行が失われていた問題を修正。
- テストが参照するサンプルファイルと期待値の不整合を修正。

### Limitations

- **PDF は Markdown 変換の対象外です。** 利用している `pdf-extract` がページ単位の文字列しか返さず、フォント情報・文字座標を公開していないため、表・装飾・リンクを復元できません。`format: 'markdown'` を指定してもプレーンテキストと同じ内容になります。
- XLSX の取り消し線・ハイライトは未対応です。
- PPTX の番号付きリストは箇条書きとして出力されます。

## [0.1.1]

- 初期リリース。PDF / DOCX / XLSX / PPTX / TXT のテキスト抽出。
